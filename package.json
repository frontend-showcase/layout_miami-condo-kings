{
  "name": "layout_miami-condo-kings",
  "version": "1.0.0",
  "description": "Miami Condo Kings Landing Page",
  "homepage": "https://frontend-showcase.github.io/layout_miami-condo-kings",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "parcel build src/index.html --dist-dir dist --public-url ./",
    "clean": "rimraf dist",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.15.4",
    "cpx": "^1.5.0",
    "parcel": "^2.15.4",
    "rimraf": "^6.0.1",
    "sass": "^1.90.0"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
